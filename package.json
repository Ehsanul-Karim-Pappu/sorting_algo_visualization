{
  "name": "sorting-algorithm-visualization",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check algorithms.js"
  },
  "engines": {
    "node": ">=18"
  }
}
