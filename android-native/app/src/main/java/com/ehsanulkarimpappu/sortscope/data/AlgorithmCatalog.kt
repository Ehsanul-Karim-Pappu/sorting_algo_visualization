package com.ehsanulkarimpappu.sortscope.data

import com.ehsanulkarimpappu.sortscope.model.AlgorithmDefinition
import com.ehsanulkarimpappu.sortscope.model.AlgorithmFamily
import com.ehsanulkarimpappu.sortscope.model.ComplexityInfo

object AlgorithmCatalog {
    val algorithms = listOf(
        algorithm(
            id = "bubble",
            name = "Bubble Sort",
            shortName = "Bubble",
            family = AlgorithmFamily.FOUNDATIONAL,
            description = "Repeatedly compare neighboring values and move the larger one toward the unsorted edge.",
            invariant = "After each pass, the largest remaining value is fixed at the right edge.",
            complexity = complexity("O(n)", "O(n²)", "O(n²)", "O(1)"),
            stable = true,
            inPlace = true,
            accent = 0xFF13D8BA.toInt(),
            code = listOf("repeat passes", "compare adjacent values", "swap when left > right", "shrink unsorted range"),
        ),
        algorithm(
            id = "cocktail",
            name = "Cocktail Shaker Sort",
            shortName = "Cocktail",
            family = AlgorithmFamily.FOUNDATIONAL,
            description = "Sweep in both directions so large and small values travel toward their final edges in one cycle.",
            invariant = "Each bidirectional cycle fixes at least one value at both ends.",
            complexity = complexity("O(n)", "O(n²)", "O(n²)", "O(1)"),
            stable = true,
            inPlace = true,
            accent = 0xFF30C7E8.toInt(),
            code = listOf("sweep left to right", "swap inverted neighbors", "sweep right to left", "tighten both edges"),
        ),
        algorithm(
            id = "selection",
            name = "Selection Sort",
            shortName = "Selection",
            family = AlgorithmFamily.FOUNDATIONAL,
            description = "Scan the remaining suffix, select its minimum, and place it at the next output position.",
            invariant = "The prefix contains the smallest values in final order.",
            complexity = complexity("O(n²)", "O(n²)", "O(n²)", "O(1)"),
            stable = false,
            inPlace = true,
            accent = 0xFFFFC857.toInt(),
            code = listOf("choose output position", "scan for minimum", "remember new minimum", "place minimum"),
        ),
        algorithm(
            id = "insertion",
            name = "Insertion Sort",
            shortName = "Insertion",
            family = AlgorithmFamily.FOUNDATIONAL,
            description = "Take one item at a time and insert it into the already ordered prefix.",
            invariant = "Everything left of the cursor is sorted and stable.",
            complexity = complexity("O(n)", "O(n²)", "O(n²)", "O(1)"),
            stable = true,
            inPlace = true,
            accent = 0xFFFF8B6A.toInt(),
            code = listOf("take next key", "compare with ordered prefix", "shift larger value right", "insert key"),
        ),
        algorithm(
            id = "merge",
            name = "Merge Sort",
            shortName = "Merge",
            family = AlgorithmFamily.DIVIDE_AND_CONQUER,
            description = "Split the array recursively, then merge ordered halves by repeatedly taking the smaller front value.",
            invariant = "Every completed merge replaces two sorted runs with one larger sorted run.",
            complexity = complexity("O(n log n)", "O(n log n)", "O(n log n)", "O(n)"),
            stable = true,
            inPlace = false,
            accent = 0xFF78A8FF.toInt(),
            code = listOf("split range in half", "sort left and right", "compare run fronts", "write smaller front"),
        ),
        algorithm(
            id = "quick",
            name = "Quick Sort",
            shortName = "Quick",
            family = AlgorithmFamily.DIVIDE_AND_CONQUER,
            description = "Partition values around a pivot, then recursively sort the smaller regions.",
            invariant = "After partitioning, the pivot separates values no greater from values greater than it.",
            complexity = complexity("O(n log n)", "O(n log n)", "O(n²)", "O(log n)"),
            stable = false,
            inPlace = true,
            accent = 0xFFFF6F91.toInt(),
            code = listOf("choose pivot", "compare value with pivot", "move value into left partition", "place pivot and recurse"),
        ),
        algorithm(
            id = "quick-three",
            name = "Three-Way Quick Sort",
            shortName = "3-Way Quick",
            family = AlgorithmFamily.DIVIDE_AND_CONQUER,
            description = "Partition into values smaller than, equal to, and greater than the pivot—especially effective for duplicates.",
            invariant = "The active range is divided into < pivot, = pivot, unknown, and > pivot regions.",
            complexity = complexity("O(n)", "O(n log n)", "O(n²)", "O(log n)"),
            stable = false,
            inPlace = true,
            accent = 0xFFFF5C7C.toInt(),
            code = listOf("choose pivot", "compare unknown value", "grow lower or upper region", "recurse outside equal region"),
        ),
        algorithm(
            id = "heap",
            name = "Heap Sort",
            shortName = "Heap",
            family = AlgorithmFamily.TREE_AND_GAP,
            description = "Build a max heap, repeatedly move its root to the sorted suffix, and restore the heap.",
            invariant = "The active prefix is a max heap and the suffix is final.",
            complexity = complexity("O(n log n)", "O(n log n)", "O(n log n)", "O(1)"),
            stable = false,
            inPlace = true,
            accent = 0xFF9A89FF.toInt(),
            code = listOf("build max heap", "compare parent and children", "promote larger child", "extract root"),
        ),
        algorithm(
            id = "shell",
            name = "Shell Sort",
            shortName = "Shell",
            family = AlgorithmFamily.TREE_AND_GAP,
            description = "Run insertion-style passes over decreasing gaps so distant inversions disappear early.",
            invariant = "After a pass, every gap-spaced subsequence is ordered.",
            complexity = complexity("O(n log n)", "≈ O(n^1.5)", "O(n²)", "O(1)"),
            stable = false,
            inPlace = true,
            accent = 0xFF56C9A4.toInt(),
            code = listOf("choose gap", "take gapped key", "shift larger gapped value", "reduce gap"),
        ),
        algorithm(
            id = "timsort",
            name = "TimSort",
            shortName = "TimSort",
            family = AlgorithmFamily.HYBRID,
            description = "Discover natural runs, extend short ones with insertion sort, and merge them into a stable result.",
            invariant = "The run stack contains individually sorted ranges waiting for balanced merges.",
            complexity = complexity("O(n)", "O(n log n)", "O(n log n)", "O(n)"),
            stable = true,
            inPlace = false,
            accent = 0xFF4DD6A5.toInt(),
            code = listOf("discover natural run", "extend short run", "push run", "merge balanced runs"),
        ),
        algorithm(
            id = "introsort",
            name = "IntroSort",
            shortName = "IntroSort",
            family = AlgorithmFamily.HYBRID,
            description = "Start with Quick Sort, fall back to Heap Sort at the recursion limit, and finish tiny ranges with Insertion Sort.",
            invariant = "Each range uses the fastest safe strategy for its size and remaining depth budget.",
            complexity = complexity("O(n log n)", "O(n log n)", "O(n log n)", "O(log n)"),
            stable = false,
            inPlace = true,
            accent = 0xFFFF9F43.toInt(),
            code = listOf("check range and depth", "partition with quick sort", "fallback to heap sort", "finish small range"),
        ),
        algorithm(
            id = "counting",
            name = "Counting Sort",
            shortName = "Counting",
            family = AlgorithmFamily.DISTRIBUTION,
            description = "Count each integer value, accumulate positions, and write items back without comparing pairs.",
            invariant = "The count table records exactly how many output positions belong to each value.",
            complexity = complexity("O(n + k)", "O(n + k)", "O(n + k)", "O(n + k)"),
            stable = true,
            inPlace = false,
            accent = 0xFF2EC4B6.toInt(),
            code = listOf("count each value", "accumulate counts", "place items stably", "copy output back"),
        ),
        algorithm(
            id = "radix",
            name = "Radix Sort",
            shortName = "Radix",
            family = AlgorithmFamily.DISTRIBUTION,
            description = "Stably group non-negative integers by one digit at a time, from least significant to most significant.",
            invariant = "After each pass, values are ordered by all digits processed so far.",
            complexity = complexity("O(d(n + 10))", "O(d(n + 10))", "O(d(n + 10))", "O(n + 10)"),
            stable = true,
            inPlace = false,
            accent = 0xFF22B8E6.toInt(),
            code = listOf("choose digit place", "count digit buckets", "place items stably", "advance digit"),
        ),
        algorithm(
            id = "bucket",
            name = "Bucket Sort",
            shortName = "Bucket",
            family = AlgorithmFamily.DISTRIBUTION,
            description = "Distribute values into ranges, sort each small bucket, then concatenate the buckets.",
            invariant = "Every value in an earlier bucket is no greater than every value in a later bucket.",
            complexity = complexity("O(n + k)", "O(n + k)", "O(n²)", "O(n + k)"),
            stable = true,
            inPlace = false,
            accent = 0xFF35C98B.toInt(),
            code = listOf("choose bucket ranges", "distribute values", "sort each bucket", "concatenate buckets"),
        ),
        algorithm(
            id = "bitonic",
            name = "Bitonic Sort Network",
            shortName = "Bitonic",
            family = AlgorithmFamily.NETWORK,
            description = "Use a fixed compare-exchange network to build and merge opposite-direction sequences, including arbitrary lengths.",
            invariant = "Each network stage orders fixed lane pairs in a known direction.",
            complexity = complexity("O(n log² n)", "O(n log² n)", "O(n log² n)", "O(log n)"),
            stable = false,
            inPlace = true,
            accent = 0xFFC6F66F.toInt(),
            code = listOf("build opposite directions", "choose comparator distance", "compare fixed lane pair", "exchange for direction"),
        ),
    )

    val byId = algorithms.associateBy { it.id }

    private fun algorithm(
        id: String,
        name: String,
        shortName: String,
        family: AlgorithmFamily,
        description: String,
        invariant: String,
        complexity: ComplexityInfo,
        stable: Boolean,
        inPlace: Boolean,
        accent: Int,
        code: List<String>,
    ) = AlgorithmDefinition(
        id = id,
        name = name,
        shortName = shortName,
        family = family,
        description = description,
        invariant = invariant,
        complexity = complexity,
        stable = stable,
        inPlace = inPlace,
        accent = accent,
        pseudocode = code,
    )

    private fun complexity(best: String, average: String, worst: String, space: String) =
        ComplexityInfo(best, average, worst, space)
}
